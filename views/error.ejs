<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error | Kala-Kriti Art Gallery</title>
  <%- include('partials/base-head') %>
</head>
<body class="bg-gray-50">
  <!-- Header -->
  <%- include('partials/header') %>
  
  <!-- Error Section -->
  <section class="py-16">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto bg-white p-8 rounded-lg shadow-md">
        <div class="flex items-center justify-center mb-6">
          <div class="w-16 h-16 bg-red-100 text-red-500 rounded-full flex items-center justify-center">
            <i class="fas fa-exclamation-circle text-2xl"></i>
          </div>
        </div>
        
        <h1 class="text-2xl md:text-3xl font-bold text-center text-gray-800 mb-4">Something went wrong</h1>
        
        <% if (locals.message) { %>
          <p class="text-center text-gray-600 mb-6"><%= message %></p>
        <% } else { %>
          <p class="text-center text-gray-600 mb-6">We encountered an unexpected error. Please try again later.</p>
        <% } %>
        
        <% if (locals.error && process.env.NODE_ENV !== 'production') { %>
          <div class="mt-6 p-4 bg-gray-100 rounded-lg overflow-auto">
            <h3 class="text-sm font-semibold text-gray-700 mb-2">Error details:</h3>
            <pre class="text-xs text-gray-600 whitespace-pre-wrap"><%= error.stack || error %></pre>
          </div>
        <% } %>
        
        <div class="flex justify-center mt-8">
          <a href="/" class="bg-black hover:bg-gray-800 text-white px-6 py-3 rounded-md mr-4 transition-colors">
            Go to Homepage
          </a>
          <a href="javascript:history.back()" class="bg-gray-200 hover:bg-gray-300 text-gray-800 px-6 py-3 rounded-md transition-colors">
            Go Back
          </a>
        </div>
      </div>
    </div>
  </section>
  
  <!-- Footer -->
  <%- include('partials/footer') %>
</body>
</html> 